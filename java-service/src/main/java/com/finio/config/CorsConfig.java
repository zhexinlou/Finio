package com.finio.config;

// CORS is now configured in SecurityConfig.
// This class is kept empty to avoid conflicts.

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
}
